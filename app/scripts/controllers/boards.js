'use strict';

/**
 * @ngdoc function
 * @name statsdsuApp.controller:BoardsCtrl
 * @description
 * # BoardsCtrl
 * Controller of the statsdsuApp
 */
angular.module('statsdsuApp')
  .controller('BoardsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
